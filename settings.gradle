pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // This is the critical fix to allow downloading OpenCV
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MCQ Grader"
include ':app'
